name: CI

on:
  create:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: |
        echo ${{ github.GITHUB_REF }}
        echo ${{ github.GITHUB_SHA }}
        echo ${{ github.event.create.GITHUB_REF }}
        echo ${{ github.event.create.GITHUB_SHA }}
